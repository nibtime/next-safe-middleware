{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "yarn clean:libs && yarn build:libs && yarn workspaces foreach -pvi run dev",
    "test": "yarn workspaces foreach -pv run test",
    "start": "yarn start:e2e",
    "clean:libs": "yarn workspaces foreach -p --no-private run clean",
    "build:libs": "yarn workspaces foreach -pvit --no-private run build",
    "build:e2e": "yarn workspaces foreach -pvitR --from e2e run build",
    "build:e2e:vercel": "yarn test && yarn build:e2e",
    "start:e2e": "yarn clean:libs && yarn build:e2e && yarn workspace e2e start",
    "version:changeset": "yarn changeset version",
    "release:changeset": "yarn build:libs && yarn changeset publish"
  },
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "@changesets/changelog-github": "^0.4.3",
    "@changesets/cli": "^2.21.0",
    "@types/eslint": "^8",
    "@types/prettier": "^2",
    "eslint": "^8.9.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  },
  "author": "nibtime",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nibtime/next-safe-middleware"
  },
  "homepage": "https://github.com/nibtime/next-safe-middleware#readme",
  "bugs": {
    "url": "https://github.com/nibtime/next-safe-middleware/issues"
  }
}
